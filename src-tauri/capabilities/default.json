{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities granted to the main launcher window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "os:default",
    "shell:default"
  ]
}
